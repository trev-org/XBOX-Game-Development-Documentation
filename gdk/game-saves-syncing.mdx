---
title: "Understanding the Game Saves sync flow"
sidebarTitle: "Sync flow"
description: "The six-step Game Saves synchronization flow, offline behavior, connection loss and lock scenarios, and power loss."
icon: "arrows-rotate"
---

This article describes the six-part synchronization flow for Game Saves. Behavior might differ slightly depending on the Game Saves API you use, but the general flow for a user-initiated save is the same.

<Steps>
  <Step title="Title start">
  </Step>
  <Step title="Connection check">
  </Step>
  <Step title="Data ownership check">
  </Step>
  <Step title="Syncing data with the cloud">
  </Step>
  <Step title="Gameplay loop">
  </Step>
  <Step title="Session end">
  </Step>
</Steps>

## Title start

When the title launches, the first step is to determine the user Game Saves is being managed for. If the user has an active game session on another device, a system dialog asks the user to switch devices. If the user switches, the other device syncs its latest local Game Saves data to the new device.

If you want data to be local and bound to the device, you can use a machine-only provider by passing a `null` user handle into the Game Saves APIs.

After the title selects the user, it initializes Game Saves with that user's handle. During initialization, Game Saves connects to the Xbox network to prepare the user's storage space.

| API | Game Saves provider initialization function |
| --- | --- |
| `XGameSave` | `XGameSaveInitializeProvider()` |
| `XGameSaveFiles` | `XGameSaveFilesGetFolderWithUIAsync()` |

Relevant system dialog: [Looks like you're signed in on another device](/gdk/game-saves-dialogues#looks-like-youre-signed-in-on-another-device).

For information about user models, see [User models](/gdk/game-saves-developer-guide#user-models). For storage, see [Game Saves storage systems](/gdk/game-saves-storage-systems).

<Frame>
  ![Flowchart of the first step in the sync process.](/images/sync-flow-1-title-start.jpg)
</Frame>

## Connection check

Next, the title determines whether it can connect to the Xbox network to confirm that syncing to the cloud is possible. If the title can't connect, the user gets an option to retry or resume the title in offline mode.

Relevant system dialogs:

- [We couldn't sync your data with the cloud](/gdk/game-saves-dialogues#we-couldnt-sync-your-data-with-the-cloud)
- [We couldn't get your latest saved data](/gdk/game-saves-dialogues#we-couldnt-get-your-latest-saved-data)

### Offline behavior

| Xbox network access | Data sync pattern |
| --- | --- |
| Yes | Normal cloud sync flow. Continue to data-ownership check. |
| No | **Offline mode:** The title saves data to the user's local Game Saves storage, but the current session doesn't sync with the cloud. |

<Frame>
  ![Flowchart of the connection check step in the sync process.](/images/sync-flow-2-connection-check.jpg)
</Frame>

## Data ownership check

After the connection is confirmed, the title determines whether the device has the right to modify the data. If it does, it can acquire a lock to the title. For a title to obtain and release a lock, it requires access to Xbox services.

The title checks whether another device holds a lock to the user's data. Another device might still retain a lock because it's:

- Disconnected from the Xbox network and unable to call `WebReleaseLock`.
- Uploading a large Game Save and unable to call `WebReleaseLock`.

If another device holds the lock, the user has the following options:

- Forcefully acquire the lock from the other device, causing the other device to lose session data.
- Try to acquire the lock normally again.
- Cancel acquiring the lock.

If the user cancels, the title launches in offline mode.

Relevant system dialog: [Your other Xbox console is taking a long time to sync to the cloud](/gdk/game-saves-dialogues#your-other-xbox-console-is-taking-a-long-time-to-sync-to-the-cloud).

For monitoring lock acquisition over the network, see [Inspecting Game Saves network traffic](/gdk/game-saves-tools#inspecting-game-saves-network-traffic).

<Frame>
  ![Flowchart of the data ownership check in the sync process.](/images/sync-flow-3-data-ownership-check.jpg)
</Frame>

## Syncing data with the cloud

After the title acquires the lock on the user's data, it syncs that data with the cloud. The title then does one of the following:

- Upload the local Game Saves data.
- Download cloud data and update the local Game Saves data.
- Take no action.
- Resolve any data conflicts.

The title determines this by comparing two timestamps:

- **Local save time** — when this device last wrote a save.
- **Cloud save time** — when the cloud copy was last updated.

### The four sync outcomes

| Local vs. cloud | Action | What it means |
| --- | --- | --- |
| Neither changed | Do nothing | Data matches in both locations. No new progress. |
| Only local changed | Upload local → cloud | User played offline. Send the newer local game-save data to the cloud. |
| Only cloud changed | Download cloud → local | User played on another device. Get their game-save data from the cloud. |
| Both changed | Conflict → prompt the user | Each location shows progress the other doesn't. User resolves. |

<Note>
  If a download from the cloud occurs, a sync dialog appears: [Syncing data for ...](/gdk/game-saves-dialogues#syncing-data). If the user cancels via this dialog, the title continues in offline mode.
</Note>

### Changed and save states

This scenario assumes the user is playing on two devices, Device A and Device B.

- **Local changed** — The user made progress while offline on Device A (for example, leveled up to 126 on Device A without an internet connection).
- **Cloud changed** — Progress was uploaded from Device B since the user last synced Device A (for example, leveled up to 99 on Device B and uploaded to the cloud).
- **Both changed** — The user played offline on Device A and also played on Device B since they last synced Device A. Device A is level 126, cloud is level 99.

If there's a conflict, the system prompts the user to resolve it via [Which one do you want to use?](/gdk/game-saves-dialogues#which-one-do-you-want-to-use).

### Atomicity

Game Saves are atomic at the **container (folder) level**. If the cloud folder includes a blob that the matching local folder doesn't contain, the system treats the two folders as separate. This difference creates a conflict.

<Frame>
  ![Flowchart of the cloud sync in the sync process.](/images/sync-flow-4-sync-data-with-cloud.jpg)
</Frame>

## Gameplay loop

The _gameplay loop_ is the active part of the game session. It begins after the title finishes syncing save data with the cloud.

During the active session, the title reads and writes save data locally. All data created or updated during this period stays in Game Saves local storage until the system performs a cloud sync.

The gameplay loop ends when the user's game session ends — for example, when the console or PC triggers a state change such as suspension, termination, sign out, or another system event.

### Periodic synchronization to the cloud

Every 30 minutes of gameplay, the title attempts to automatically sync to the cloud.

| Game Saves API | Action | When |
| --- | --- | --- |
| `XGameSaveFiles`, No-code cloud saves | Upload local → cloud | 30 minutes elapsed since the game wrote to the designated save area. |
| `XGameSave` | Upload local → cloud | Every 30 minutes of gameplay. |

<Frame>
  ![Flowchart of the gameplay loop interactions with Game Saves.](/images/sync-flow-5-gameplay-loop.jpg)
</Frame>

## Session end

After the game session ends, the title uploads the new data to the cloud. The device attempts to upload data when it has a connection. After the upload succeeds, the lock is released.

To see the lock release on the Xbox network for debugging, see [Inspecting Game Saves network traffic](/gdk/game-saves-tools#inspecting-game-saves-network-traffic).

<Frame>
  ![Flowchart of session end behavior in the sync process.](/images/sync-flow-6-session-end.jpg)
</Frame>

## Connection loss and lock scenarios

Data written during connection loss is stored in Game Saves local storage and uploaded after the connection resumes. However, if the user attempts to use another device, the current device can't release the lock because the connection was lost. The user can force a transfer of the lock to another device. Depending on when these lock transfers occur, a title can experience differing behavior.

### Connection loss before title start

> **When:** The user launches a title without a connection.

The title provides the option to launch in offline mode. When the device reconnects and the cloud contains newer data, the title treats the local data as stale. In this case, the title ends the session by terminating the app with a suspend operation.

Relevant system dialog: [We need to close this game or app](/gdk/game-saves-dialogues#we-need-to-close-this-game-or-app).

### Connection loss during the gameplay loop

> **When:** The user is in an active session and the title still holds the lock. The connection is lost and the title allows gameplay to continue.

| Offline device | Lock state on reconnect | Title action on reconnect | User experience |
| --- | --- | --- | --- |
| Console | Still holds lock | Continue saving locally. | Saves go to local storage. |
| Console | Lock taken by another device | The title terminates. | Exit to Home screen. |
| PC | Still holds lock | Continue saving locally. | Saves go to local storage. |
| PC | Lock taken by other device | Gameplay resumes as normal. Conflict resolution is done on the next sync. | Gameplay resumes. |

### Connection loss before session end (terminate or suspend)

> **When:** The player quits/suspends while the device is disconnected. The device still has unsynced local data.

| Offline device | Time of connection loss | Lock state on reconnect | Device action on reconnect |
| --- | --- | --- | --- |
| Console | Before termination | Still holds lock | Data uploaded and lock released. |
| Console | Before termination | Lock taken by other device | Data goes through merge resolution on next title start. |
| Console | Before suspend | Still holds lock | Data uploaded and lock released. |
| Console | Before suspend | Lock taken by other device | Title terminated; data goes through merge resolution on next title start. |
| PC | Before termination | Still holds lock | Data uploaded and lock released. |
| PC | Before termination | Lock taken by other device | Data goes through merge resolution on next title start. |
| PC | Before suspend | Still holds lock | Data uploaded and lock released. |
| PC | Before suspend | Lock taken by another device | Data goes through merge resolution on next title start. |

## Power loss

Power loss acts similarly to connection loss. The main difference is that any save operations that were active are lost.