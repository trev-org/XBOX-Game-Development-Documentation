---
title: "GDK Get Started"
sidebarTitle: "GDK Get Started"
description: "Start here for XBOX GDK development — overview, dev PC setup, SDK install, and samples."
icon: "rocket"
---

This is **step 2 of the guided path**. You should have completed [ID@XBOX onboarding](/onboarding) before you begin — you cannot download the GDK without it.

By the end of this section you will know what the GDK is, have Visual Studio \+ the Windows SDK \+ the GDK installed on a properly configured dev PC, and have built and run a sample.

## What is the GDK?

The **XBOX Game Development Kit (GDK)** is Microsoft's unified toolchain for building games that target XBOX Series X|S, XBOX One, and Windows PC from a single codebase. It supersedes the legacy XDK and replaces UWP as the recommended way to ship Microsoft Store games.

The GDK provides:

- **Gaming Runtime** — the core platform APIs (`XGameRuntime`, `XGameSave`, `XStore`, `XUser`, `XPackage`, ...).
- **Platform Extensions** — console-specific features for XBOX Series X|S and XBOX One.
- **Visual Studio integration** — the `Gaming.Desktop.x64` and `Gaming.Xbox.Scarlett.x64` platforms.
- **Command-line tools** — `makepkg`, `xbapp`, `wdapp`, `SubmissionValidator`, and the MicrosoftGame.config editor.
- **Samples** — reference titles for graphics, audio, networking, and store integration.

<Tip>
  See the [GDK glossary](/gdk/glossary) for terms and acronyms, and the [API reference](/reference/system-overview) for XGameRuntime, XGameUI, XAppCapture, XMem, XPackage, XStore, and more.
</Tip>

## Do these in order

<Steps>
  <Step title="Install the GDK toolchain" icon="download">
    Confirm your dev PC meets requirements, then install the Windows SDK, Visual Studio, and the GDK in order.

    <Card title="Install the GDK" icon="arrow-right" href="/gdk/download-install">
      Requirements, prerequisites, and step-by-step install.
    </Card>
  </Step>
  <Step title="Build a sample" icon="code">
    Open one of the shipped GDK samples in Visual Studio and confirm your environment builds and deploys cleanly. This validates every step above.

    <Card title="GDK samples" icon="arrow-right" href="/gdk/samples">
      Reference samples that ship with the GDK.
    </Card>
  </Step>
</Steps>

## Next: build your first title

Once a sample builds and runs, move on to creating your own project.

<Card title="Build your first title" icon="arrow-right" href="/gdk/developing-new-titles">
  Pick a Visual Studio template and get your own GDK project running.
</Card>